# Server Configuration
NODE_ENV=production
PORT=8080

# Database Configuration (REQUIRED for production)
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/portfolio_tracker

# JWT Configuration (REQUIRED for production - generate a secure random string)
JWT_SECRET=your_jwt_secret_key_here_change_in_production
JWT_EXPIRE=7d

# Stock API (optional - used for real-time stock prices)
STOCK_API_URL=http://stock-api:5001

# Frontend
FRONTEND_URL=http://localhost:3000
